"use client";
type Props = { page: number; totalPages: number; totalItems: number; limit: number; onPage: (page: number) => void };
export default function Pagination({ page, totalPages, totalItems, limit, onPage }: Props) { if (!totalItems) return null; const start = (page - 1) * limit + 1, end = Math.min(page * limit, totalItems), first = Math.max(1, Math.min(totalPages - 4, page - 2)), pages = Array.from({ length: Math.min(totalPages, 5) }, (_, index) => first + index); return <div className="flex flex-wrap items-center justify-between gap-3 text-sm text-slate-400"><p>Showing {start}–{end} of {totalItems.toLocaleString()} companies</p><div className="flex gap-1"><button disabled={page === 1} onClick={() => onPage(page - 1)} className="rounded px-3 py-1.5 disabled:opacity-40 hover:bg-slate-800">Previous</button>{pages.map((number) => <button key={number} onClick={() => onPage(number)} className={`rounded px-3 py-1.5 ${page === number ? "bg-emerald-400 font-bold text-slate-950" : "hover:bg-slate-800"}`}>{number}</button>)}<button disabled={page === totalPages} onClick={() => onPage(page + 1)} className="rounded px-3 py-1.5 disabled:opacity-40 hover:bg-slate-800">Next</button></div></div>; }
